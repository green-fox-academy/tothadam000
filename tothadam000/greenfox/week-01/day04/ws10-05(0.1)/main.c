#include <stdio.h>

/*
store your age, height(for example: 1.85 in meters) as different variables
print them out, using only one printf function
use correct variables for each
*/

int main() {
    int age = 31;
    float height = 1.85;

        printf("Age:%d \t Height:%.02f \n", age,height);


	return 0;
}
